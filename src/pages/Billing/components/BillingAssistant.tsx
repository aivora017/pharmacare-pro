/**
 * AI Billing Assistant side panel (Tier 1).
 * Shows: drug interaction warnings, upsell suggestions (probiotics with antibiotics),
 *        near-expiry alerts, overdue refill reminders for the selected customer.
 * Copilot: call aiService in the background when items change; show actionable cards.
 */
export function BillingAssistant() {
  return null // TODO (Copilot): implement AI assistant side panel
}
