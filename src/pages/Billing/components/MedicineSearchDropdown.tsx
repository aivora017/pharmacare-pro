/**
 * Medicine search results dropdown for POS.
 * Copilot: call medicineService.search(query, { in_stock_only: true })
 * On result click: addItem to cartStore with FEFO batch selected.
 * Show: name, generic name, stock, price, expiry warning if near expiry.
 * Highlight search term in results.
 */
// TODO (Copilot): implement this component
export function MedicineSearchDropdown() {
  return null // placeholder
}
